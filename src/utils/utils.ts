
// export function format(first: string, middle: string, last: string): string {
//   return (
//     (first || '') +
//     (middle ? ` ${middle}` : '') +
//     (last ? ` ${last}` : '')
//   );
// }


// export class Get {
//   public static format(firstName: string, lastName: string): string {
//     return firstName + lastName;
//   }
// }



// import {StringFormat} from '../utls';

// StringFormat.format()